Чтоб включить инструменты разработчика, нужно нажать F12

Команда Debugger
Выполнение кода можно также приостановить 
с помощью команды debugger прямо изнутри самого кода:

function hello(name) {
  let phrase = `Привет, ${name}!`;

  debugger;  // <-- здесь выполнение прерывается

  say(phrase);
}